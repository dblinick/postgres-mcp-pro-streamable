# PostgreSQL Database Configuration
# Use either POSTGRES_* (recommended) or DATABASE_URI (legacy)
POSTGRES_USERNAME=your_username
POSTGRES_PASSWORD=your_password
POSTGRES_HOST=your_db_host
POSTGRES_PORT=5432
POSTGRES_DATABASE=your_database
# SSL modes: disable, prefer, require, verify-ca, verify-full
POSTGRES_SSL_MODE=disable

# Optional API key for Streamable HTTP transport
MCP_API_KEY=your_api_key

# Optional host/origin allowlist for DNS rebinding protection
# Comma-separated values, include host:port if needed
MCP_ALLOWED_HOSTS=192.168.5.30,192.168.5.30:8000
MCP_ALLOWED_ORIGINS=http://192.168.5.30:8000

# Disable DNS rebinding protection (not recommended)
MCP_DISABLE_DNS_REBINDING=false

# Streamable HTTP settings
STREAMABLE_HTTP_HOST=0.0.0.0
STREAMABLE_HTTP_PORT=8000
